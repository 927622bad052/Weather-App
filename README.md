💡 Project Name: Weather App (Using Python + API)

🔧 What it does:
	•	Takes a city name as input.
	•	Fetches current weather data using the OpenWeatherMap API.
	•	Displays temperature, weather condition (e.g., cloudy, sunny), and humidity.

🧰 Tools/Libraries:
	•	Python
	•	requests library
	•	OpenWeatherMap API (free signup)
